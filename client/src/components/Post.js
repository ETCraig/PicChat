import React, {Component} from 'react';

import Styled from 'styled-components';

class Post extends Component {
    render() {
        return(
            <div>

            </div>
        );
    }
}

export default Post;